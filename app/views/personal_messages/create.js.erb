var conversation = $('#conversations-list').find("[data-conversation-id='" + "<%= @conversation.id %>" + "']");
conversation.find('.messages-list').find('ul').append("<%= j(render 'personal_messages/personal_message', personal_message: @personal_message, user: current_user) %>");
conversation.find('textarea').val('');