<li class="left clearfix">
	<span class="chat-img1 pull-left">
		<%= gravatar_for(personal_message.user, size:20) %>
	</span>
	<div class="chat-body1 clearfix">
		<p><%= personal_message.body %></p>
		<div class="chat_time pull-right"><%= time_ago_in_words(personal_message.created_at).chomp('utes') %> ago</div>
	</div>
</li>
