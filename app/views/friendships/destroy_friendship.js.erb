$('#following_<%= @follow.id %>').remove();
$('#following h3').html("You are following <%= pluralize(current_user.active_friends.count, 'person') %>");
$('#user_<%= @follow.id %> .status_buttons').html("<%= escape_javascript(render partial: 'users/status_buttons', locals: { user: @follow }) %>");